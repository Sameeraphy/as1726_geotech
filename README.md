# as1726_geotech

Python tools for AS1726 geotechnical site investigations.

Quickstart

Install in editable mode and run tests:

```bash
python -m pip install --upgrade pip
pip install -e .
pip install pytest
pytest -q
```

This project is in early development; modules are placeholders and will be expanded.
